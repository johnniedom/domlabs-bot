"""Voice subpackage — opt-in transcription + TTS.

Imports are deferred inside each helper so the package still imports cleanly
when the optional ``[voice]`` extras (and their deps) aren't installed.
"""
